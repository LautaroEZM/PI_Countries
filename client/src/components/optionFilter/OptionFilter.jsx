import React from "react";
import style from './OptionFilter.module.css';

const OptionFilter = ({ activity }) => {
    return (
        <div>
            {activity.name}
        </div>
    );
};

export default OptionFilter;
